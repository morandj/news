{
  "private": true,
  "scripts": {
    "build": "pwa export --routes /about,/blog,/",
    "start": "sirv build -s",
    "watch": "pwa watch"
  },
  "dependencies": {
    "file-loader": "^2.0.0",
    "ganalytics": "^3.0.0",
    "preact": "latest",
    "preact-compat": "latest",
    "preact-router": "latest",
    "sirv-cli": "latest",
    "webpack": "^4.20.2"
  },
  "devDependencies": {
    "@pwa/cli": "^0.3.2",
    "@pwa/plugin-sw-workbox": "^0.3.1",
    "@pwa/preset-preact": "^0.1.0"
  }
}
